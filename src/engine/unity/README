

更新帧
当前帧
当前时间
detal时间
操作数组


UNITY接口

新建对象
参数：（字符串）资源名称


销毁对象
参数：（字符串）对象路径

设置父对象：
参数：对象路径
参数：目标对象路径

移动对象
参数：（字符串）对象路径
参数：当前位置
参数：目标位置

旋转对象
参数：（字符串）对象路径
参数：当前角度
参数：目标角度

缩放对象
参数：（字符串）对象路径
参数：当前大小
参数：目标大小


