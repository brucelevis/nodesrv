#!bin/lneox

if not arg[1] then
    print('stopsrv xxx')
    return
end

local srvname = arg[1]
local file = io.open(string.format('proc/%s/pid', srvname))
local pid = tonumber(file:read())
file:close()

print('pid is', pid)

local command = string.format('kill -2 %d', pid)
os.execute(command)

